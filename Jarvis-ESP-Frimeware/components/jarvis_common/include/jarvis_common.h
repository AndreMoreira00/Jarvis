// jarvis_common.h — API utilitaria compartilhada (alem dos tipos em jarvis_types.h).
#pragma once

#include "jarvis_types.h"

// Loga o banner de boot (nome + versao de protocolo). Sinal de vida do jarvis_common.
void jarvis_log_banner(void);
