// wifi_stream.c — STUB. Real: SoftAP efemero + WebSocket MJPEG sob demanda.
#include "wifi_stream.h"
#include "esp_log.h"

void wifi_stream_init(void)
{
    ESP_LOGI("wifi_stream", "stub init (SoftAP / MJPEG pendente)");
}
