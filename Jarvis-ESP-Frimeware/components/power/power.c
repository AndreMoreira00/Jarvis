// power.c — STUB. Real: light/deep sleep, wake-on-GPIO (INT do IMU), ADC de bateria.
#include "power.h"
#include "esp_log.h"

void power_init(void)
{
    ESP_LOGI("power", "stub init (sleep / bateria pendente)");
}
