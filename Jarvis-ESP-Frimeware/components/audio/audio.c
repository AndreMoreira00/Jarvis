// audio.c — STUB. Real: I2S mic (16k PCM) + MAX98357A; Opus fica com o consumidor.
#include "audio.h"
#include "esp_log.h"

void audio_init(void)
{
    ESP_LOGI("audio", "stub init (I2S mic + MAX98357A pendente)");
}
