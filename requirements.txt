# Dependencias de runtime do Jarvis.
# Sem versoes fixas nesta fase; a reprodutibilidade vira do uv.lock (Onda 3).

# Visao computacional e captura
opencv-python
mediapipe
numpy

# Audio: reconhecimento de fala (mic) e sintese de voz
SpeechRecognition
PyAudio          # backend de microfone do SpeechRecognition (precisa de PortAudio no SO)
edge-tts
pygame

# IA (Google Gemini)
google-generativeai

# Config e credenciais (Google Photos via OAuth)
python-dotenv
requests
google-auth
google-auth-oauthlib
